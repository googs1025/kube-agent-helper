---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterconfigs.k8sai.io
spec:
  group: k8sai.io
  names:
    kind: ClusterConfig
    listKind: ClusterConfigList
    plural: clusterconfigs
    singular: clusterconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              kubeConfigRef:
                description: KubeConfigRef is the reference to a Secret containing a kubeconfig for the remote cluster.
                properties:
                  key:
                    type: string
                  name:
                    type: string
                required:
                - key
                - name
                type: object
              prometheusURL:
                description: PrometheusURL is the Prometheus endpoint accessible from within the remote cluster.
                type: string
              description:
                type: string
            required:
            - kubeConfigRef
            type: object
          status:
            properties:
              phase:
                enum:
                - Connected
                - Error
                type: string
              message:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
