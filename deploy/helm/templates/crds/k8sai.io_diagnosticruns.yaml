---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: diagnosticruns.k8sai.io
spec:
  group: k8sai.io
  names:
    kind: DiagnosticRun
    listKind: DiagnosticRunList
    plural: diagnosticruns
    singular: diagnosticrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              modelConfigRef:
                type: string
              skills:
                items:
                  type: string
                type: array
              target:
                properties:
                  labelSelector:
                    additionalProperties:
                      type: string
                    type: object
                  namespaces:
                    items:
                      type: string
                    type: array
                  scope:
                    enum:
                    - namespace
                    - cluster
                    type: string
                required:
                - scope
                type: object
            required:
            - modelConfigRef
            - target
            type: object
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              findingCounts:
                additionalProperties:
                  type: integer
                type: object
              findings:
                items:
                  properties:
                    dimension:
                      type: string
                    resourceKind:
                      type: string
                    resourceName:
                      type: string
                    resourceNamespace:
                      type: string
                    severity:
                      type: string
                    suggestion:
                      type: string
                    title:
                      type: string
                  required:
                  - dimension
                  - severity
                  - title
                  type: object
                type: array
              message:
                type: string
              phase:
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                type: string
              reportId:
                type: string
              startedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
